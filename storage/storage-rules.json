{
  "rules": {
    "users": {
      "{userId}": {
        
        ".read": "request.auth != null && request.auth.uid == userId",
        ".write": "request.auth != null && request.auth.uid == userId"
      }
    },
    
    ".read": false,
    ".write": false
  }
}